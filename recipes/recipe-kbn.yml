---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# image will be published to ghcr.io/<user>/<name>
name: gregos-kbn
description: Greg's personalized KDE-based desktop environment using the Bazzite kernel and Nvidia drivers.

base-image: ghcr.io/ublue-os/aurora-hwe-nvidia
image-version: stable

modules:
  - from-file: common.yml
